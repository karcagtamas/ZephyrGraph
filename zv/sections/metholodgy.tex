\begin{frame}{Metholodgy}
    \begin{alertblock}{Key Steps}
        \begin{enumerate}[<+->]\itemsep9pt
            \item The user defining the \textbf{text-based cause-effect graph} structure.
            \item Parsing the input into a \textbf{graph model}.
            \item Transforming the model into \textbf{logical formulas} (e.g., DNF).
            \item \textbf{Simplifing} and refining formulas to reduce complexity.
            \item Translating the forumlas into a \textbf{decision table}.
            \item \textbf{Exporting} the results into GPT format.
        \end{enumerate}
    \end{alertblock}
\end{frame}

\begin{frame}{Metholodgy}
    \begin{alertblock}{Highlights}
        \begin{itemize}[<+->]\itemsep9pt
            \item \textbf{Minimal manual intervention} during the process.
            \item Supports \textbf{large and complex rule} systems.
        \end{itemize}
    \end{alertblock}
    \begin{alertblock}{Outcome}
        \begin{itemize}[<+->]\itemsep9pt
            \item Seamless transition from textual inputs to actionable test cases.
        \end{itemize}
    \end{alertblock}
\end{frame}